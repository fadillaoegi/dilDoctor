import { View, Text } from "react-native";
import React from "react";

// type Props = {}

const Spacing = ({ height = 20, width }) => {
  return <View style={{ height: height, width: width }} />;
};

export default Spacing;
